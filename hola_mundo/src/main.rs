//Definir función con fn seguido del nombre de la función (en este caso main)
// seguido de paréntesis y un juego de llaves
fn main() {
  //Función println! es una función macro (se verá más adelante)
    println!("Hola Mundo"); // el mensaje va dentro de los paréntesis usando comillas dobles y se termina con ;
    // Antes de ejecutar, compilar con rustc "nombre archivo sin comillas" o cargo build en una terminal
    // Para ejecutar el ejecutable ./nombre_archivo dentro de la ubicación del mismo desde una terminal
    // También se puede ejecutar con cargo run en una terminal
}
